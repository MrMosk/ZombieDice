package com.company;

import java.util.Random;


public class Main {
    //cups need to randomly generate the variable needed for brains, bullets, and runners each on three separate difficulty's easy, medium, hard
    // these variables need a genorator between 1 & 6  each one set in the three difficulty's
    //player gets three die to roll depending on a random number between 1 and 3 will select the difficulty of the die

    public class cup{
        boolean zombie = true;
        Random rand = new Random();
        int n = rand.nextInt(3) + 1;
        int d = rand.nextInt(6) + 1;














    }





    public static void main(String[] args) {
	// write your code here
    }
}
